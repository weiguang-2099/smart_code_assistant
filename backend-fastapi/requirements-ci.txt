# Lightweight CI dependencies - excludes heavy packages that are mocked in tests.
# For full dev/production dependencies, use requirements.txt instead.
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.1.1
python-dotenv==1.0.1
aiomysql==0.2.0
aiosqlite>=0.20.0
sqlalchemy[asyncio]==2.0.35
alembic==1.13.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
argon2-cffi>=21.3.0
python-multipart==0.0.12
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov>=7.0.0
httpx==0.27.2
zhipuai>=2.1.5
# LangChain dependencies - narrow ranges to avoid backtracking while staying compatible
langchain>=0.3.16,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langchain-core>=0.3.0,<0.4.0
langgraph>=0.2.0,<0.3.0
langchain-community>=0.3.16,<0.4.0
# PDF parsing and format conversion
markdown-it-py>=3.0.0
mdit-py-plugins>=0.4.0
# Code GraphRAG - neo4j driver only (chromadb/sentence-transformers mocked in tests)
neo4j>=5.15.0
tenacity>=8.2.0
tiktoken>=0.5.0
# OpenTelemetry - core only; exporters not needed in CI (no OTLP/Jaeger endpoint).
# The telemetry.py code falls through to ConsoleSpanExporter when exporters are missing.
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
# Sentry (optional - only initialised if SENTRY_DSN is set)
sentry-sdk[fastapi]>=2.0.0
